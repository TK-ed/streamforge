name: Docker Build Check

on:
  push:
    branches: ["main", "fix/*", "feature/*"]
  pull_request:
    branches: ["main"]

jobs:
  docker-build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      # ---- API SERVICE BUILD ----
      - name: Build API Gateway
        run: |
          docker build -t api-gateway:ci ./services/api_gateway

      # ---- WORKER BUILD ----
      - name: Build Worker
        run: |
          docker build -t worker:ci ./workers

      # ---- OPTIONAL: Infra validation ----
      - name: Validate docker-compose (if exists)
        run: |
          test -f docker-compose.yml && docker compose config || echo "No compose file"
